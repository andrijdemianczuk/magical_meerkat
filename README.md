# magical_meerkat

_Placeholder — description to come._

## Setup

This project uses [uv](https://docs.astral.sh/uv/) with Python 3.14.

```bash
uv venv --python 3.14
source .venv/bin/activate
uv pip install -r requirements.txt   # once dependencies exist
```
